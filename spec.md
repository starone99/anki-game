# Anki Game — Spec

## 1. 비전

Anki 일본어 덱(.apkg)을 불러오면, 단어들이 화면 위에서 동시에 떨어지는 타이핑 게임으로 플레이할 수 있다. 로마자/한국어 발음/히라가나/의미 중 원하는 입력 모드를 선택해 타이핑하면 단어가 제거되고, 틀리거나 바닥에 닿으면 HP가 깎이고 해당 카드는 복습 큐에 쌓인다. 덱 없이도 내장 히라가나/가타카나 덱으로 바로 플레이 가능하다. 학습과 게임이 자연스럽게 연결된다.

---

## 2. 핵심 게임 루프

```
덱 로드 (or 내장 덱 선택) → 세션 설정 → 게임 시작
    ↓
카드 풀에서 단어를 뽑아 화면 상단에 생성 (동시 N개)
    ↓
단어가 아래로 떨어짐 (속도: 난이도에 따라 조절)
    ↓
유저가 타이핑 → 매칭되는 단어 제거 + 점수
    ↓
바닥 도달 or 오답 → HP -1 + 복습 큐 추가
틀린 카드는 3~5장 후 재등장
    ↓
HP 0 → 게임 오버
카드 소진 → 클리어
결과 화면 표시
```

### 난이도별 동시 단어 수
| 난이도 | 동시 단어 수 | 낙하 속도 |
|--------|-------------|-----------|
| 쉬움   | 2개         | 느림      |
| 보통   | 4개         | 보통      |
| 어려움 | 6개         | 빠름      |

### 세션 설정
- 세션 크기: 기본 20장, 10~50 범위 조절 가능
- 카드 선택: due 정보 있으면 due 순서 우선, 없으면 순서대로
- HP: 기본 5칸, 시작 전 설정 가능

---

## 3. 입력 시스템

### 입력 모드 4가지 (게임 시작 전 고정)

| 모드 | 입력 예 (比べる / くらべる) |
|------|---------------------------|
| 로마자 | `kuraberu` |
| 한국어 발음 | `쿠라베루` |
| 히라가나 직접 | `くらべる` |
| 의미 (한국어) | `비교하다` |

### 매칭 로직
- 첫 글자부터 실시간 하이라이트 (자동 매칭, 클릭 선택 없음)
- 여러 카드가 같은 글자로 시작하면 모두 동시 강조, 타이핑하며 좁혀짐
- 의미 모드: 쉼표 구분 복수 정답 허용 ("싸다, 포장하다" → 둘 다 정답)
- 히라가나 힌트: 기본 숨김, Tab키로 토글

### 한국어 발음 매핑
히라가나 → 한국어 음가 고정 테이블 사용
예: か→카, き→키, く→쿠, け→케, こ→코

---

## 4. 덱 파싱 & 데이터 모델

### .apkg 구조
```
.apkg (ZIP)
  └─ collection.anki21b (zstd 압축 SQLite) — 신규 포맷
  └─ collection.anki2 (SQLite) — 구버전 포맷
  └─ 0, 1, 2... (미디어 파일)
  └─ media (미디어 매핑 JSON)
```

### 실제 덱 카드 구조 (reference.txt 기준)
```
field 0 (Front): 한국어 의미   예) "비교하다", "싸다, 포장하다"
field 1 (Back):  한자+HTML     예) "比べる<div>くらべる</div>"
```

### 파싱 전략
```
Back 필드:
  - HTML 태그 앞 텍스트 → 한자/단어 (화면 표시용)
  - <div> or <br> 안 텍스트 → 히라가나 읽기 (정답 기준)

Front 필드:
  - HTML 태그 제거 → 한국어 의미
  - 쉼표로 분리 → 복수 정답 배열
```

### 내장 덱
- 히라가나 46자 (あいうえお ~ ん)
- 가타카나 46자 (アイウエオ ~ ン)
- 혼합 모드

---

## 5. UI/UX 설계

### 화면 구성 (3개 화면)

**① 덱 로드 화면**
- .apkg 파일 드래그&드롭 or 파일 선택
- "내장 덱으로 시작" 버튼 (히라가나 / 가타카나 / 혼합)
- 덱 정보 표시 (카드 수, 덱 이름)
- 세션 크기 설정
- 입력 모드 선택
- 난이도 선택
- HP 설정
- 시작 버튼

**② 게임 화면**
- 상단: HP바 + 점수 + 남은 카드 수
- 중앙: 단어들이 위에서 아래로 떨어짐 (CSS Animation)
  - 각 단어 카드: 한자 크게 + 히라가나 작게 (Tab으로 토글)
  - 타이핑 중인 카드 하이라이트
- 하단: 입력 상태 표시 (별도 입력창 없음, 키보드 직접 입력)

**③ 결과 화면**
- 점수 / 정답률
- 틀린 카드 목록 (한자 + 읽기 + 의미)
- 다시하기 / 복습 모드 버튼

---

## 6. 기술 아키텍처

### 스택
- React + Vite + TypeScript
- CSS Animation (떨어지는 단어)
- sql.js (브라우저에서 SQLite 직접 읽기)
- fflate (ZIP 압축 해제)
- zstd-codec (anki21b zstd 해제)

### 배포
- 완전 클라이언트 사이드 (서버 없음)
- .apkg 파일 브라우저에서 직접 파싱 (서버 업로드 없음)
- GitHub Pages or Vercel 정적 배포

---

## 7. 개발 계획

### Stream C — PoC (최우선, 병렬)
- **C1.** 브라우저 zstd 해제 + sql.js SQLite 읽기
  - DoD: reference.txt 로드 → 카드 10개 파싱 성공
- **C2.** 한국어 IME composition 입력 처리
  - DoD: 한국어 타이핑 시 composition 이슈 없이 매칭

### Stream A — 코어 엔진 (C1 통과 후 순차)
- **A1.** .apkg 파서
  - DoD: anki21b + anki2 양쪽 포맷 파싱, HTML 제거, 복수 정답 분리
- **A2.** 카드 선택 & 세션 매니저
  - DoD: 세션 크기, due 순서, 복습 큐, 3~5장 후 재등장 검증
- **A3.** 입력 매칭 엔진
  - DoD: 4개 모드 각각 정답/오답 유닛 테스트 통과

### Stream B — UI (A1 완료 후 병렬)
- **B1.** 덱 로드 화면
  - DoD: 드래그&드롭 로드, 내장 덱 선택, 설정 저장
- **B2.** 게임 화면
  - DoD: 단어 낙하, 하이라이트, HP바, 힌트 토글 동작
- **B3.** 결과 화면
  - DoD: 점수/정답률/틀린 카드 목록 표시

### 실행 순서
```
1단계: C1, C2 동시
2단계: A1
3단계: A2 + B1 병렬
4단계: A3 + B2 병렬
5단계: 통합 테스트
6단계: B3
7단계: 배포
```

---

## 8. Project DoD

```
[ ] 1. .apkg 로드
    - reference.txt 드래그&드롭으로 로드 성공
    - 카드 수, 덱 이름 정상 표시
    - anki21b (zstd) + anki2 양쪽 포맷 모두 처리

[ ] 2. 카드 파싱
    - 한자, 히라가나, 한국어 의미 정상 추출
    - HTML 태그 제거 완료
    - 쉼표 구분 복수 정답 처리

[ ] 3. 게임 루프
    - 단어 동시 2~6개 떨어짐 (난이도별)
    - 타이핑 시 실시간 하이라이트
    - 바닥 도달 시 HP -1 + 복습 큐 추가
    - HP 0 → 게임 오버
    - 세션 카드 소진 → 클리어

[ ] 4. 입력 모드 4가지 모두 동작
    - 로마자 / 한국어 발음 / 히라가나 / 의미
    - 한국어 IME 입력 정상 처리 (composition 이슈 없음)

[ ] 5. 복습 큐
    - 틀린 카드 3~5장 후 재등장
    - 결과 화면에 틀린 카드 목록 표시

[ ] 6. 결과 화면
    - 점수, 정답률, 틀린 카드 목록
    - 다시하기 동작

[ ] 7. 내장 덱
    - 히라가나/가타카나/혼합 선택 가능
    - 덱 로드 화면에서 "내장 덱으로 시작" 버튼 표시
    - 동일한 게임 루프 동작

[ ] 8. 배포
    - GitHub Pages or Vercel 정적 배포 완료
    - 모바일 브라우저에서도 기본 동작
```
